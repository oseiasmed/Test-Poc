const universalList = []

let universalExpList = universalList[Math.floor(Math.random() * universalList.length)]

export const receiveUniversalList = universalExpList


const emailList = ["yahoo", "gmail", "hotmail", "outlook", "aolmail", "protonmail","zohomail"]

let emailExpList = emailList[Math.floor(Math.random() * emailList.length)]

export const receiveEmailList = emailExpList

